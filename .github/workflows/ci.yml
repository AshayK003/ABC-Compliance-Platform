name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          cd /home/runner/work/ABC-Compliance-Platform/ABC-Compliance-Platform
          pip install --only-binary :all: -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Run pytest
        env:
          DATABASE_URL: postgresql+asyncpg://abc:abc@localhost:5432/abc_dashboard
          SECRET_KEY: ci-only-secret-key-0123456789abcdef0123456789abcdef
        run: |
          cd /home/runner/work/ABC-Compliance-Platform/ABC-Compliance-Platform
          pytest -q -m "not e2e"

      - name: Run pip-audit
        run: |
          pip install --only-binary :all: pip-audit
          pip-audit -r requirements.txt --desc

  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: |
          cd frontend
          npm ci --ignore-scripts

      - name: Run lint (ESLint)
        run: |
          cd frontend
          npm run lint

      - name: Run oxlint
        run: |
          cd frontend
          npx oxlint src/

      - name: Run impeccable
        run: |
          cd frontend
          npx -y impeccable@2.3.2 detect src/ --json || echo "::warning::impeccable reported style advisories (non-blocking)"

      - name: Type check
        run: |
          cd frontend
          npm run typecheck

      - name: Build
        run: |
          cd frontend
          npm run build

      - name: Run Vitest
        run: |
          cd frontend
          npx vitest run

      - name: Run Playwright tests
        run: |
          cd frontend
          npx playwright install --with-deps chromium
          npx playwright test

      - name: Run npm audit
        run: |
          cd frontend
          npm audit --audit-level=high --ignore-scripts